{
  "name": "@sellfuse/ai-gateway-server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "dev": "tsx src/server.ts",
    "start": "node dist/server.js"
  },
  "dependencies": {
    "@sellfuse/ai-gateway": "*",
    "@sellfuse/validation": "*",
    "zod": "^4.5.4"
  }
}
